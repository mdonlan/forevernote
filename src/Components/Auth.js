import { loadUserNotebooks } from '../../API';

export default class {
    constructor (dispatch) {
        this.dispatch = dispatch;

        this.watcher();
    }

    watcher () {
        
    }

    setUserData (user) {
        
    }
    
    clearUserData () {
        
    }
}